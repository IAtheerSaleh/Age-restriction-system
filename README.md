 Age Restriction System

A Python program that checks if a user meets the required age for access.

 Features
- Takes user age as input
- Validates age requirements
- Displays an error message if age is not allowed

 Language
Python
